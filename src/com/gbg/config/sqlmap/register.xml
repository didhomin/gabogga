<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="rg">
  
    <select id="emailCheck" parameterClass="String" resultClass="int">
        select count(email)
        from users
        where email=#email#
    </select>
    <insert id="register" parameterClass="usersDto">
        insert into users (user_id, email, password, tel, gender, type, reg_date, state)
        values (SEQ_USERS_USERS_ID.nextval, #email#, #password#, #tel#, #gender#, '1', sysdate, '1')
    </insert>
    <insert id="emailAuth" parameterClass="String">
        update users
        set state=2
        where email=#email#
    </insert>
    <select id="login" parameterClass="java.util.Map" resultClass="usersDto">
        select user_id userId, email, password, tel, gender, type, reg_date regDate, state
        from users
        where email=#email# and password=#password#
    </select>
    <update id="passReset" parameterClass="java.util.Map">
        update users
        set password=#password#,state=3
        where email=#email#
    </update>
    <update id="passModify" parameterClass="java.util.Map">
        update users
        set password=#password#,state=2
        where email=#email#
    </update>
</sqlMap>